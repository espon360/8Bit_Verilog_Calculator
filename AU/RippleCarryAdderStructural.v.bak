// Ripple Carry Adder Model
module RippleCarryAdderStructural (
	input [7:0] A, B,
	input add_subtract,
	output [7:0] S,
	output Cout);
	wire [8:0] C;
	assign C[0] = add_subtract;
	assign Cout = C[8];